# -----------------------------------------------
# 모두의 문철 - 프론트엔드 환경변수 템플릿
# .env.local로 복사 후 실제 값 입력: cp .env.local.example .env.local
# -----------------------------------------------

# 백엔드 API 주소 (개발: http://localhost:8080 / 프로덕션: https://api.your-domain.com)
NEXT_PUBLIC_API_URL=http://localhost:8080
